name: Build and Push Frontend & Backend Docker Images

on:
  push:
    branches:
      - main
    # If you want it to run only when frontend/backend changes, you can uncomment:
    # paths:
    #   - "frontend_project/**"
    #   - "backend_project/**"

jobs:
  build-and-push-backend:
    name: Build & Push Backend Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./backend_project
          file: ./backend_project/Dockerfile
          push: true
          tags: |
            mounika30913/home-tutor-backend:latest

  build-and-push-frontend:
    name: Build & Push Frontend Image
    runs-on: ubuntu-latest
    needs: build-and-push-backend   # waits until backend job finishes

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push frontend Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend_project
          file: ./frontend_project/Dockerfile
          push: true
          tags: |
            mounika30913/home-tutor-frontend:latest
